# Flask_Task1
A lightweight Flask web application built to master backend fundamentals. This project captures user input through URL query parameters, performs real-time string manipulation (Upper Case conversion).
